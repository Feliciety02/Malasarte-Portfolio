# Social Media Graphics Assets

Each portfolio project has its own folder, named with the project slug.

Add images using a numeric prefix so the bento gallery keeps a predictable order:

```text
01-primary-post.png
02-campaign-layout.png
03-story-asset.png
```

Supported formats: SVG, PNG, JPG, JPEG, and WebP.
